import 'package:flutter/cupertino.dart';

// ignore: camel_case_types
class contactphone extends StatelessWidget {
  const contactphone({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("contactphone"),
    );
  }
}